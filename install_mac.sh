#!/bin/bash
# W@Home Hive — macOS Installer
# Redirects to the unified installer which handles both Linux and macOS.
# Usage: curl -sSL https://wathome.akataleptos.com/static/install_mac.sh | bash
curl -sSL https://wathome.akataleptos.com/static/install.sh | bash
